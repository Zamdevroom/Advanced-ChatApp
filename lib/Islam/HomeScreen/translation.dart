import 'package:flutter/material.dart';

class Translationsurahview extends StatelessWidget {
  const Translationsurahview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Translation"),
      ),
    );
  }
}
